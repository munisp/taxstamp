# Mission-Critical Assurance Remediation Ledger

**Target:** Dirty local working tree on `devin/1787593004-tax-stamp-platform` over baseline `09bd499532a0cf829d5bd35232443131c7fb31d6`.

The lifecycle statuses below follow the user-endorsed assurance protocol. `VERIFIED_FIXED` means the original local detector was rerun successfully. `EXTERNAL_BLOCKED` remains an absolute production-release block.

| ID | Severity | Lifecycle state | Finding, affected path, and impact | Root cause | Complete correction and regression proof | Re-test evidence and disposition |
|---|---|---|---|---|---|---|
| F-001 | High | `VERIFIED_FIXED` | Generated dependency artifacts could enter source control and inflate build/deployment context: mobile `node_modules`, Rust `target`, formatter scratch files. | Ignore policy did not cover all generated tool outputs. | Added ignore rules; removed only temporary formatter artifacts; kept source and lockfiles. | `git status` no longer reports generated artifacts; Docker image rebuilt. |
| F-002 | Medium | `VERIFIED_FIXED` | Python build isolation could choose a future `setuptools` release despite a reproducible-package claim. | Build-system dependency was an open lower-bound range. | Pinned `setuptools==84.0.0`; added `build==1.2.2.post1`, `make package`, CI step, and `test_build_configuration.py`. | `make package` built `taxstamp-1.0.0-py3-none-any.whl`; unit regression passed. |
| F-003 | High | `VERIFIED_FIXED` | CI omitted Go, Rust, and Expo client verification, allowing a Python-only green result to mask polyglot defects. | Workflow scope did not enumerate all checked language modules. | Added Go list/vet/race, Rust fmt/Clippy/test, and mobile frozen install/typecheck jobs; added workflow regression assertions. | Actionlint passed; Go and Rust local gates passed; Expo gate passed. |
| F-004 | High | `VERIFIED_FIXED` | Local APISIX configuration explicitly disabled upstream certificate verification. | `ssl_verify: false` remained in the local gateway declaration. | Removed the insecure override. | Local Compose syntax passed; rebuilt disposable stack passed health/Kafka checks; unauthenticated `/v1/readiness` returned 401. This is not production TLS proof. |
| F-005 | High | `VERIFIED_FIXED` | Expo SDK 52 production audit reported critical/high transitive advisories, including `tar`. | Stale framework dependency graph. | Upgraded Expo-compatible packages to SDK 57, React 19.2.3, React Native 0.86.2; added pnpm workspace `uuid: 11.1.1` override; added mobile audit CI gate. | Frozen install, `tsc`, Expo config evaluation, and `pnpm audit --prod --audit-level=low` all passed with no known vulnerabilities. |
| F-006 | Medium | `VERIFIED_FIXED` | Initial Alembic migration was outside local formatter/lint coverage and failed formatter check. | Makefile scope omitted `migrations`; generated DDL was unformatted. | Formatted `0001_initial_schema.py`; expanded format/lint target to migrations; added narrow documented ignores only for generated long DDL/statement-count diagnostics. | `make lint` passed; full migration upgrade/downgrade/upgrade and migration integration tests passed. |
| F-007 | High | `VERIFIED_FIXED` | Separate PWA production audit exposed high/moderate advisories and deprecated inline pnpm policy was ignored. | Old resolved graph and unsupported package-manifest pnpm policy. | Updated compatible direct dependency ranges; moved overrides and Wouter patch to `pnpm-workspace.yaml`; added audit script, CI workflow, and Vitest policy assertions. | Frozen install, typecheck, 7 Vitest tests, build, actionlint, and production audit all passed; separate WebDev checkpoint `75b9913c`. |
| F-008 | Low | `VERIFIED_FIXED` | CI did not prove that package build and PWA audit commands survive workflow edits. | Commands were not asserted by regression tests. | Added Python CI workflow assertions and PWA workflow regression test. | Unit/Vitest tests and actionlint passed. |
| F-009 | High | `VERIFIED_FIXED` | A bearer-authenticated verification caller could submit an arbitrary `device_id`; the unbound value controlled replay/velocity evidence and persisted audit data. | `/v1/verify` authenticated the principal but never compared its subject to `VerifyRequest.device_id`. | Added `Actor.require_subject`; the verification route now denies a signed request whose `device_id` differs from the authenticated principal subject. Updated valid lifecycle/integration fixtures to use canonical identity. | New E2E negative test rejects the signed cross-device claim with 403; targeted real PostgreSQL/Redis tests and the full suite passed. |
| F-010 | High | `VERIFIED_FIXED` | TLS enforcement trusted `X-Forwarded-Proto` from any source whenever proxy-header trust was enabled. | The middleware used a client-controlled forwarded header without a source-network trust boundary. | Added `trusted_proxy_cidrs`, validates CIDR syntax, requires CIDRs for staging/production proxy-header trust, and accepts forwarded transport headers only from a configured client network. | Unit regressions prove default and unlisted-client denial plus listed-proxy success; full suite, Compose checks, and local gateway validation passed. |
| F-011 | Medium | `VERIFIED_FIXED` | HTTP metrics could label time series with arbitrary raw unmatched paths, risking unbounded Prometheus cardinality. | Middleware read the route before router resolution and fell back to `request.url.path`. | Metrics now resolve the route after dispatch and label unmatched paths as the constant `<unmatched>`. | Unit regression makes two hostile unmatched paths and proves neither appears in the metrics output; full suite passed. |
| F-012 | Medium | `VERIFIED_FIXED` | Staging/production settings allowed an empty trusted-host list, bypassing `TrustedHostMiddleware`. | `trusted_hosts` was optional in all environments. | Configuration now requires non-wildcard trusted hosts outside development/test. | Production configuration regressions reject missing and wildcard host policy; full suite passed. |
| F-013 | Medium | `VERIFIED_FIXED` | A completed idempotency record could be replayed by a different principal using the same scope, key and payload hash. | The lookup selected by scope/key and replayed the stored body without comparing `principal_id`. | The durable claim path now rejects a record owned by another principal before payload or response replay. No migration was required. | New real-PostgreSQL integration test proves cross-principal replay is refused; full suite passed. |
| F-014 | Low | `VERIFIED_FIXED` | Bandit found an optimization-removable assertion in the storage-attestation converter’s output path. | A Python `assert` was used as a runtime guard. | Replaced it with an explicit fail-closed error path. | Converter tests, full Bandit scan, and the full suite passed. |
| B-001 | Critical | `EXTERNAL_BLOCKED` | No authoritative, approved Nigerian excise, VAT, product, cutoff, currency-exponent, exception, or rate-governance source is integrated. | Required business/legal authority is unavailable. | No safe code-only correction exists; values must not be invented. | Obtain source owner approval, versioned ingestion/governance, effective-date and exception tests, and legal/business acceptance. Release remains blocked. |
| B-002 | Critical | `EXTERNAL_BLOCKED` | FIRS/NAFDAC/SON/Customs, bank/PSP, Mojaloop, and TigerBeetle live/sandbox provider effects have not been exercised. | No approved credentials, accounts, sandbox agreement, real client, or counterparty operating procedure. | Current code is explicitly configuration-dependent/fail-closed where applicable. | Provision approved non-production credentials and official sandboxes; run signed positive/negative/timeout/replay/reconciliation evidence. Release remains blocked. |
| B-003 | Critical | `EXTERNAL_BLOCKED` | Storage encryption, KMS/HSM, backups, restore, rotation, access review, and recovery are only attestation/configuration gates. | No cloud/KMS/HSM account or operator-produced evidence. | Synthetic strict checker intentionally never claims a live provider verification. | Provide real key policy/HSM evidence, encrypted PostgreSQL/Redis/OpenSearch scope, snapshot/backup evidence, access review, rotation and restore drill. Release remains blocked. |
| B-004 | Critical | `EXTERNAL_BLOCKED` | No production-shaped deployment, signed artifact/SBOM, rollback, DR, incident, performance/load, or controlled restore evidence exists. | No approved target environment or operations ownership was supplied. | Local Docker image and disposable Compose validation are retained as limited evidence only. | Execute target-environment deploy/canary/rollback, backup restore, RTO/RPO, alert, capacity and incident drills. Release remains blocked. |
| B-005 | High | `EXTERNAL_BLOCKED` | Keycloak/APISIX/Permify evidence proves only local availability and anonymous gateway denial. | No production claim/role/tenant model, TLS/mTLS material, application decision integration, or admin lifecycle. | Local APISIX insecure verification override was removed. | Complete authenticated/negative tenant-role matrix under real TLS/mTLS; establish realm/key rotation, tuple lifecycle, decision logs, WAF/rate limits, and operational ownership. |
| B-006 | High | `EXTERNAL_BLOCKED` | TigerBeetle boundary has no protocol client/cluster credentials and Rust implementation is not a Python-worker runtime call. | Adapter remains a durable intent/reconciliation foundation, not a live transfer integration. | Durable intent/outbox state and deterministic branch tests are documented as non-live evidence. | Implement authenticated provider client and account mapping; prove status lookup-before-retry, remote timeout, restart, reconciliation and compensation against a real cluster. |
| B-007 | High | `EXTERNAL_BLOCKED` | Missing Nigerian legal/DPO/CBN/payment-scheme applicability and control-owner decisions. | Engineering artifacts cannot decide regulated scope or certify compliance. | Documentation keeps engineering/legal boundary explicit. | Obtain written applicability matrix, approved policies, named owners, retention/rights decisions, and counsel/compliance sign-off. |
| B-008 | Medium | `EXTERNAL_BLOCKED` | Rust dependency advisory scan could not complete locally because compatible `cargo-audit 0.21.1` cannot parse current RustSec CVSS 4 metadata on the Rust 1.75 toolchain. | Scanner/toolchain compatibility mismatch; local Rust crate has no demonstrated third-party provider runtime path. | Rust fmt/Clippy/tests passed; failed scanner output is preserved as an evidence limitation, not a pass. | Upgrade/validate the scanner/toolchain or use an approved compatible vulnerability-scanning service and record the clean result. |
| B-009 | High | `EXTERNAL_BLOCKED` | OpenSearch, openAppSec, Dapr, Fluvio, and lakehouse declarations are not deployed, integrated, or operationally evidenced. | Integration manifest intentionally declares unavailable/configured-not-verified boundaries. | No capability is represented as live. | Confirm required architecture, then implement and test security, tenant, retention, encryption, replay and operations controls. |
| B-010 | Medium | `EXTERNAL_BLOCKED` | Offline verification/device trust, printer/press controls, optical/taggant validation, and ML authenticity are not implemented. | Product capability gaps, not hidden stubs. | Capability registry explicitly rejects/unavailable status. | Business owner must defer in writing or fund vendor/device integration and field acceptance/security evidence. |
| B-011 | Medium | `EXTERNAL_BLOCKED` | `handle_notify` creates an auditable `notification.recorded` event but has no external notification-delivery adapter. | Delivery channel, recipient policy and escalation workflow have not been implemented. | Current handler does not falsely assert delivery; it records local handling only. | Confirm notification requirement and implement provider/channel integration, delivery status, retries, failure escalation and operator evidence if required. |

## Test-double and anti-simulation disposition

The release-evidence integration suite used actual disposable PostgreSQL, Redis, Kafka, Keycloak, APISIX, Permify, Prometheus, and Grafana containers where those local services were in scope. Deterministic test doubles remain only in narrowly scoped unit/branch tests, including the transport-neutral TigerBeetle client path and a unit-level `Mock` in worker-main tests. They are not evidence of provider interoperability, remote durability, credentials, settlement, security, or production readiness. No local test was weakened, deleted, skipped, or converted to a log-only assertion to close a finding.
